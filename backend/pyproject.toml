[project]
name = "backend"
version = "0.1.0"
description = "PreLegal FastAPI backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
]
